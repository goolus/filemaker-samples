<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Event: <%= event.title %></title>
  </head>
  <body>
    <div id="content">
      <h1><%= event.title %></h1>
      <h2>開催日: <%= event.pubDate %></h2>
      <p>主催者: <%= event.organizer %></p>
      <p>本文: <%= event.body %></p>
      <p><img src="<%= event.image %>"></p>
    </div>
  </body>
</html>
