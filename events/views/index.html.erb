<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Event</title>
  </head>
  <body>
    <ul>
      <% events.each do |event| %>
        <li><a href="/show?id=<%= event.id %>"><%= event.pubDate %>&nbsp;:<%= event.title %></a></li>
      <% end %>
    </ul>
  </body>
</html>

<% events %>
